/* Run: node build.cjs. No packages required. Reads one editable record per play. */
const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm');
const root=__dirname;
const e=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const order=['alternating-elbow-shooting','laker-drill','trap-drill','closeout-contain','cut-read-3v3','advantage-break','hit-find-get','screen-read-3v3','five-out','dribble-at-backdoor','screen-away','drag-screen','press-break','zone-buster','gap-help-recover','full-court-man','baseline-help','ball-screen-defense','run-and-jump','box-blob','baseline-screen-slip','baseline-stack','baseline-four-flat','sideline-box','made-basket-entry'];
const cats={drills:'Drills',offense:'Offensive Plays / Strategies',defense:'Defensive Plays / Strategies',inbounds:'Inbounding Plays'};
const plays=fs.readdirSync(path.join(root,'content')).filter(f=>f.endsWith('.js')).map(file=>{const context={window:{}};vm.runInNewContext(fs.readFileSync(path.join(root,'content',file),'utf8'),context);return context.window.CRIMSON_PLAY;}).sort((a,b)=>(order.indexOf(a.slug)<0?999:order.indexOf(a.slug))-(order.indexOf(b.slug)<0?999:order.indexOf(b.slug)));
const seen=new Set();for(const p of plays){if(!p||!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(p.slug)||seen.has(p.slug))throw Error('Invalid or duplicate slug');seen.add(p.slug);if(!cats[p.category])throw Error(p.slug+': unknown category');if(!p.options?.length)throw Error(p.slug+': missing reads');for(const o of p.options){if(o.frames[0].t!==0||o.frames.at(-1).t!==1)throw Error(p.slug+': incomplete timeline');let prev=-1;for(const f of o.frames){if(f.t<=prev)throw Error(p.slug+': unordered frames');prev=f.t;for(const v of Object.values(f.pos))if(v.length!==2||v.some(x=>!Number.isFinite(x)))throw Error(p.slug+': invalid coordinates');for(const value of Object.values(f.balls||{main:f.ball})){if(typeof value==='string'?!f.pos[value]:!Array.isArray(value)||value.length!==2||value.some(x=>!Number.isFinite(x)))throw Error(p.slug+': invalid ball');}}}}
const icon="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='16' fill='%230c1420'/%3E%3Ccircle cx='32' cy='32' r='23' fill='%23ed5671'/%3E%3Cpath d='M9 32h46M32 9v46M17 14q25 18 0 36M47 14q-25 18 0 36' fill='none' stroke='%230c1420' stroke-width='3'/%3E%3C/svg%3E";
function head(title,description,prefix=''){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#0c1420"><meta name="description" content="${e(description)}"><title>${e(title)} | Crimson Playbook</title><link rel="icon" href="${icon}"><link rel="stylesheet" href="${prefix}assets/style.css"></head><body><a class="skip" href="#main">Skip to content</a><header class="topbar"><a class="brand" href="${prefix}index.html"><span class="brand-icon" aria-hidden="true">✣</span><span><b>CRIMSON</b> / PLAYBOOK</span></a><nav class="toplinks" aria-label="Main navigation"><a href="${prefix}index.html">Play library</a><a href="${prefix}coach-guide/index.html">Coach’s guide</a><span class="edition">6TH GRADE · CLUB</span></nav></header>`;}
function foot(prefix=''){return `<footer class="footer"><span>CRIMSON BASKETBALL · Play fast. Read the game. Play together.</span><a href="${prefix}coach-guide/index.html#sources">Coaching sources & rules notes ↗</a></footer></body></html>`;}
function scripts(prefix='',slug=null){return `<script src="${prefix}${slug?'content/'+slug+'.js':'assets/catalog.js'}"></script><script src="${prefix}assets/court.js"></script><script src="${prefix}assets/app.js"></script>`;}
const counts=Object.fromEntries(Object.keys(cats).map(c=>[c,plays.filter(p=>p.category===c).length]));
let home=head('Play library','A decision-based basketball playbook for a skilled, smaller sixth-grade club team.');
home+=`<div class="layout"><aside class="sidebar"><div class="sidebar-label">THE PLAY LIBRARY</div><nav class="category-nav" aria-label="Play categories"><a href="#all" data-category="all">All entries <span class="count">${plays.length}</span></a>${Object.entries(cats).map(([id,name])=>`<a href="#${id}" data-category="${id}">${e(name)} <span class="count">${counts[id]}</span></a>`).join('')}</nav><div class="sidebar-note"><strong>OUR IDENTITY</strong><p>Win space with speed.<br>Make the simple read.<br>Pressure with purpose.<br>Finish with a rebound.</p></div><div class="sidebar-note"><strong>NUMBERS ARE JOBS</strong><p>Every player learns to handle, cut, screen, help, and rebound. “5” does not have to be your tallest player.</p></div></aside><main id="main" class="library-main"><section class="hero"><div><div class="eyebrow">Built for a fast, connected team</div><h1>Play fast.<br><span>Read the game.</span></h1><p>A bigger playbook for a smaller lineup. Learn the spacing, see the defender’s choice, and make the next play together.</p><div class="hero-stats"><span><b>${plays.length}</b> entries</span><span><b>4</b> categories</span><span><b>${plays.filter(p=>!p.original).length}</b> new additions</span></div></div><a class="featured-court" href="gap-help-recover/index.html" aria-label="Explore gap help man-to-man defense"><div class="mini-svg" id="hero-court"></div><div class="featured-caption"><span><b>TEAM DEFENSE</b> / See ball & man</span><span>Explore the read ↗</span></div></a></section><div class="install-strip"><strong>START WITH THE CORE</strong><p>Five-out. Gap help. Clean entries. Add layers as the reads become automatic.</p><a href="coach-guide/index.html">Your first practices ↗</a></div><section aria-labelledby="section-title"><div class="library-toolbar"><div><h2 id="section-title">The complete playbook</h2><p id="section-subtitle">${plays.length} ways to build a fast, connected team.</p></div><label class="search-wrap"><span aria-hidden="true">⌕</span><span class="sr-only">Search plays and drills</span><input type="search" id="search" placeholder="Search plays, skills, or level…" autocomplete="off"></label></div><p id="result-count" class="sr-only" role="status" aria-live="polite"></p><div class="card-grid" id="cards"></div><noscript><p>JavaScript enables filters and animations. Open any play below to read its coaching notes.</p><ul>${plays.map(p=>`<li><a href="${p.slug}/index.html">${e(p.title)}</a></li>`).join('')}</ul></noscript><p class="footnote">${plays.filter(p=>p.original).length} refined originals + ${plays.filter(p=>!p.original).length} added entries. Defensive choices are teaching examples; live defenders can make another choice.</p></section></main></div>`;
fs.writeFileSync(path.join(root,'index.html'),home+scripts()+foot());
fs.writeFileSync(path.join(root,'assets/catalog.js'),'window.CRIMSON_CATALOG = '+JSON.stringify(plays.map(p=>({slug:p.slug,category:p.category,title:p.title,description:p.description,level:p.level,original:p.original||false,full:p.full||false,help:p.help||false,preview:p.options[0],optionsCount:p.options.length,players:p.players,sequence:p.sequence})))+';\n');
for(const [i,p] of plays.entries()){
 const prefix='../',previous=plays[(i+plays.length-1)%plays.length],next=plays[(i+1)%plays.length];let html=head(p.title,p.description,prefix);
 html+=`<main class="page" id="main"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="../index.html">Playbook</a><span>/</span><a href="../index.html#${p.category}">${e(cats[p.category])}</a><span>/</span><span>${e(p.title)}</span></nav><div class="play-heading"><div><div class="eyebrow">${e(cats[p.category])} <span aria-hidden="true"> / </span> ${e(p.level)}</div><h1>${e(p.title)}</h1><p>${e(p.description)}</p></div><div class="actions"><button id="share">Copy this read</button><button id="print">Print lesson</button></div></div><div id="share-fallback" hidden class="info-panel"><label for="share-url">Copy this link to share the selected read and moment:</label><input id="share-url" readonly style="width:100%"></div><div class="board-layout"><section class="board-panel" aria-label="Animated play diagram"><div class="board-top"><div class="label">${p.sequence?'Drill sequence':'Choose what the defense does'}</div><div class="options" id="options"></div><div class="read-box" id="read-text"></div></div><div class="court-wrap ${p.full?'full':''}"><div id="court"></div><div class="court-note"><span>${p.full?'ATTACK → RIGHT BASKET':'ATTACK ↑ TOP BASKET'} · Schematic spacing</span><span>${p.duration<5?'Teaching timeline · not an official count':'Slow teaching pace · not game timing'}</span></div></div><div class="transport"><label class="timeline"><output id="elapsed">0.0s</output><span class="sr-only">Animation timeline</span><input id="scrubber" type="range" min="0" max="1000" value="0" step="1"><span>${p.duration.toFixed(1)}s</span></label><div class="transport-buttons"><button class="primary" id="play-pause" aria-pressed="false">Play</button><button id="reset">Reset</button><button id="previous-step" aria-label="Previous teaching step">← Step</button><button id="next-step" aria-label="Next teaching step">Step →</button><label for="speed">Speed <select id="speed"><option value=".5">0.5×</option><option value="1" selected>1×</option><option value="1.5">1.5×</option></select></label></div><div class="toggles"><label title="Restart the selected animation from its setup after every replay"><input id="repeat" type="checkbox">Continuous loop</label><label><input id="paths" type="checkbox" checked>Next movement</label>${p.sequence?'':'<label><input id="matchups" type="checkbox">Matchup lines</label>'}${p.help?'<label><input id="help" type="checkbox" checked>Help-side guide</label>':''}</div></div><div class="legend"><span><i class="dot">1</i> ${p.sequence?'L1–L5 / R1–R5: starting lines':'Offense / numbered jobs'}</span>${p.sequence?'<span><i class="dot ball"></i> Three balls: A, B, C</span><span>Colors track starting group, not current line</span>':'<span><i class="dot def">X1</i> Defender</span><span><i class="dot ball"></i> Ball</span><span><i class="screen-key"></i> Planted screen</span><span>Dashed path = defender</span>'}</div></section><aside class="coach-panel" aria-label="Coaching notes"><section class="phase-panel"><div class="phase-number" id="phase-number">Step 1</div><h2 id="phase-caption">Set up</h2><p>${p.sequence?'Watch the shooter pursue the bounce, jump to secure it, and keep moving into the feed and outside return. Loop restarts this six-shot example from the original setup.':'Pause at the decision. Ask: “What did the defender do? Where is the space?”'}</p></section><section class="info-panel"><h2>Set it up</h2><p>${e(p.setup)}</p></section><section class="info-panel"><h2>Coaching cues</h2><ol>${p.cues.map(c=>`<li>${e(c)}</li>`).join('')}</ol></section><section class="info-panel"><h2>Run it at practice</h2><p>${e(p.practice)}</p></section>${p.note?`<div class="callout">${e(p.note)}</div>`:''}</aside></div><section class="step-list"><h2>${p.sequence?'Walk through the rotation':'Walk through the read'}</h2><div class="steps" id="steps"></div></section><section class="print-reads"><h2>${p.sequence?'Drill sequence':'All defensive reads'}</h2>${p.options.map(o=>`<p><strong>${e(o.label)}:</strong> ${e(o.read)}</p>`).join('')}</section><noscript><section class="info-panel"><h2>${p.sequence?'Drill sequence':'Defensive reads'}</h2>${p.options.map(o=>`<h3>${e(o.label)}</h3><p>${e(o.read)}</p><ol>${(o.steps||o.frames).map(f=>`<li>${e(f.caption)}</li>`).join('')}</ol>`).join('')}<p>Enable JavaScript to animate these teaching steps.</p></section></noscript>${p.options.some(o=>o.screens?.length)?'<div class="callout" style="margin-top:20px">Screen cue: arrive, plant, wait, cut, release. Stay stationary with arms close before contact. Give an unseen stationary defender a normal step and a moving defender time and distance to avoid the screen. The gold bar marks the screening side; do not chase a defender with a moving screen.</div>':''}${p.category==='inbounds'?'<div class="callout" style="margin-top:14px">Inbound cue: identify the safety early and release within five seconds of the ball being at the thrower’s disposal. Request a timeout before the violation only if one is available. On a designated-spot throw-in, stay within the spot; endline movement after a made basket is different.</div>':''}<details class="sources"><summary>Coaching basis & diagram notes</summary><p>${p.sequence?'Animation of the coach’s requested drill. Three balls maintain the rebound-triggered shooting rhythm.':'Original teaching adaptation for this team, informed by these coaching resources. These sources support the principles, not a claim that this exact sequence guarantees a basket.'}</p><ul>${p.sources.map(s=>`<li><a href="${e(s.url)}" target="_blank" rel="noopener">${e(s.title)}</a></li>`).join('')}</ul><p>${p.sequence?'L and R identify the starting line; player labels and colors stay the same after switching sides. Ball letters A, B and C track the three balls. Shot and rebound flight is schematic.':'Numbers describe roles, not permanent positions. Xn initially corresponds to player n; any illustrated exchange is called out. Court and screen spacing are schematic, not a rules measurement. For a live rep, opponents may recover or choose another coverage.'}</p><p>Keyboard: with focus outside the controls, Space plays/pauses and arrow keys step. Continuous loop replays the selected animation from its original setup; it does not continue player identities into a new round. Pause, stepping, or scrubbing stops playback. Switching a read resets the diagram. Copy this read saves its option and timeline position in the link.</p><a href="../coach-guide/index.html#rules">Rules and local event notes</a></details><nav class="next-prev" aria-label="More plays"><a href="../${previous.slug}/index.html">← ${e(previous.title)}</a><a href="../${next.slug}/index.html">${e(next.title)} →</a></nav></main>`;
 fs.mkdirSync(path.join(root,p.slug),{recursive:true});fs.writeFileSync(path.join(root,p.slug,'index.html'),html+scripts(prefix,p.slug)+foot(prefix));
}
// The guide is authored separately to keep basketball teaching and UI templates readable.
fs.mkdirSync(path.join(root,'coach-guide'),{recursive:true});fs.writeFileSync(path.join(root,'coach-guide/index.html'),head('Coach’s guide','An installation plan and practice guide for a smaller sixth-grade club basketball team.','../')+fs.readFileSync(path.join(root,'guide.html'),'utf8')+foot('../'));
fs.writeFileSync(path.join(root,'.nojekyll'),'');
console.log(`Built library, coaching guide, and ${plays.length} play routes. Categories: ${JSON.stringify(counts)}`);
